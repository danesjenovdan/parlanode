describe('nedbService', function() {

  beforeEach(module('parlameterCardCms'));

  it('should ...', inject(function(nedbService) {

    //expect(nedbService.doSomething()).toEqual('something');

  }));

});
