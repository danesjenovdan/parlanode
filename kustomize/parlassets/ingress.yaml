apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlassets
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-sandbox"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - parlassets-hrvaska.lb.djnd.si
        - cdn.10.parlametar.hr
      secretName: parlassets-tls
  rules:
    - host: parlassets-hrvaska.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlassets
                port:
                  number: 80
    - host: cdn.10.parlametar.hr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlassets
                port:
                  number: 80
