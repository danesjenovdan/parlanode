apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlasite
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-sandbox"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - trbovlje.parlameter.si
      secretName: parlasite-tls
  rules:
    - host: trbovlje.parlameter.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlasite
                port:
                  number: 3066
