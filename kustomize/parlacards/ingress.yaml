apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlacards-lendava
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - parlacards-lendava.lb.djnd.si
      secretName: parlacards-tls
  rules:
    - host: parlacards-lendava.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlacards-lendava
                port:
                  number: 3000
