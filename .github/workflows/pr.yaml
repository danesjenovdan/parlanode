on:
  push:
    branches:
    - dev

jobs:
  pull-requests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: pull-request-k8s
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: "k8s"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        pr_title: "Update ${{ github.ref }} from k8s"

    - name: pull-request-ajdovscina
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: "ajdovscina"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        pr_title: "Update ${{ github.ref }} from ajdovscina"

    - name: pull-request-lendava
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: "lendava"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        pr_title: "Update ${{ github.ref }} from lendava"

    - name: pull-request-hrastnik
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: "hrastnik"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        pr_title: "Update ${{ github.ref }} from hrastnik"

    - name: pull-request-ua
      uses: repo-sync/pull-request@v2
      with:
        destination_branch: "ua"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        pr_title: "Update ${{ github.ref }} from ua"
