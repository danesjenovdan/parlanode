apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlassets-slovenija-nginx
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - parlassets-slovenija.lb.djnd.si
      secretName: parlassets-slovenija-nginx-tls
  rules:
    - host: parlassets-slovenija.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlassets-slovenija-nginx
                port:
                  number: 80
