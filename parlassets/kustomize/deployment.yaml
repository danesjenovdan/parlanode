apiVersion: apps/v1
kind: Deployment
metadata:
  name: parlassets-ajdovscina-deployment
  labels:
    app: parlassets-ajdovscina
spec:
  replicas: 1
  selector:
    matchLabels:
      app: parlassets-ajdovscina
  template:
    metadata:
      labels:
        app: parlassets-ajdovscina
    spec:
      containers:
        - name: parlassets-ajdovscina-nginx
          image: parlassets-ajdovscina
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              memory: 100Mi
              cpu: 200m
            limits:
              memory: 100Mi
              cpu: 200m
