apiVersion: apps/v1
kind: Deployment
metadata:
  name: parlasite-slovenija-deployment
  labels:
    app: parlasite-slovenija
spec:
  replicas: 1
  selector:
    matchLabels:
      app: parlasite-slovenija
  template:
    metadata:
      labels:
        app: parlasite-slovenija
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: parlasite-slovenija
          image: parlasite-slovenija
          ports:
            - containerPort: 3066
          resources:
            requests:
              memory: 50Mi
              cpu: 50m
            limits:
              memory: 50Mi
              cpu: 50m
