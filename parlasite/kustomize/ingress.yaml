apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlasite-slovenija
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - parlasite-slovenija.lb.djnd.si
      secretName: parlasite-slovenija-tls
  rules:
    - host: parlasite-slovenija.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlasite-slovenija
                port:
                  number: 3066
