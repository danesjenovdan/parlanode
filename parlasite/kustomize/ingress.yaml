apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlasite-lendava
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - parlasite-lendava.lb.djnd.si
      secretName: parlasite-lendava-tls
  rules:
    - host: parlasite-lendava.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlasite-lendava
                port:
                  number: 3066
